#include<stdio.h>

int main(){

    int numb1;

    scanf("%d", &numb1);
    if(numb1 % 2 == 0)
    printf("%d is an even number", numb1);
    else if(numb1 % 2 == 1)
    printf("%d is and odd number", numb1);





    return 0;
}